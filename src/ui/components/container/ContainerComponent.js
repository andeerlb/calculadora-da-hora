import React from 'react'
import { Styled } from '../../../styles'

export function Container({ children }) {
    return (
        <Styled.Container>
            {children}
        </Styled.Container>
    )
}

